<h1>投稿一覧</h1>
<% @posts.each do |post| %>
 <h3><%= link_to post.title, post_path(post) %></h3>
 <p><%= truncate(post.body) %></p>
 <%= link_to '続きを読む', post_path(post), class: "btn-sm btn-default" %>
 <% end %>
